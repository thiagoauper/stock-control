import { ProductMovementModel } from './product-movement-model';

describe('ProductMovementModel', () => {
  it('should create an instance', () => {
    expect(new ProductMovementModel()).toBeTruthy();
  });
});
